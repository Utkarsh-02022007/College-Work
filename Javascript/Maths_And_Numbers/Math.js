
const min= 1
const max = 6



console.log(Math.floor(Math.random()*(max-min+1)+min))// this is the formula for to keep a outcome in the range used max-min=1 TO get number before decimal and used +min to keep it in the range
console.log(Math.ceil(4.5))
console.log(Math.max(4,5,6,7,8))
console.log(Math.min(3,5,1,3,7,2))
console.log(Math.floor(4.56343))
